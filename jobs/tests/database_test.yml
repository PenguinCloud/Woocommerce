---
- name: Check database connectivity
  command: "mysql -u {{ database.user }} -p {{ database.password }} -h {{ database.host }} -P  {{ database.port }}"
  register: database_connect

- name: Assert that database is connected
  assert:
    that: "'Welcome to the MySQL monitor' in database_connect.stdout"