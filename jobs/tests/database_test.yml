---
- name: Check database connectivity
  shell: "mysql -u {{ database.user }} -p {{ database.password }} -h {{ database.host }} -P  {{ database.port }}"
  changed_when: false
  ignore_errors: yes ## Just for now